# Silnik Multibota (slafy-bot @ SLAFY-BOT-SHA.txt) — instalacja:
#   uv venv .venv --python 3.12
#   uv pip install --python .venv/Scripts/python.exe -r requirements.txt
#
# hermes-agent pinowany na SHA — CI i świeże maszyny nie mają sąsiedniego
# klona. Dev lokalny może nadpisać instalacją editable:
#   uv pip install -e G:/Projects/hermes-agent
hermes-agent @ git+https://github.com/NousResearch/hermes-agent@17688f9
aiohttp
fastapi
uvicorn
httpx
pytest
playwright
mcp==1.28.1
starlette==1.3.1
numpy
edge-tts
python-multipart
