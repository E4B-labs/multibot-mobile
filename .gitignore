node_modules/
.expo/
dist/
web-build/
*.log
*.local
*.tsbuildinfo
.DS_Store

# Katalogi natywne generuje `prebuild` — u nas na serwerze EAS, nie lokalnie.
# MUSZĄ tu być: `@expo/fingerprint` pomija katalog natywny tylko wtedy, gdy jest
# ignorowany przez gita. Bez tego odcisk policzony lokalnie (bez `android/`) nie
# zgadza się z odciskiem po `prebuild` na EAS (z `android/`) i build pada na
# fazie CONFIGURE_EXPO_UPDATES z "Runtime version mismatch".
/android
/ios
